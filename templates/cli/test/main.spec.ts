import {
  describe,
  // expect,
  it,
  // spyOn
} from 'bun:test'

describe('main', () => {
  it.todo('should print hello world', async () => {
    // const consoleSpy = spyOn(console, 'log')

    // await import('../src/index.ts')
    // expect(consoleSpy).toHaveBeenCalledWith('Hello, world!')

    // consoleSpy.mockRestore()
  })
})
